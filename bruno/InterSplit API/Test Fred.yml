info:
  name: "Test Fred "
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "fred@test.com",
        "name": "Fred",
        "password": "test12356",
        "preferred_currency": "GBP"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
