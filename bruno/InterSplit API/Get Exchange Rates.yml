info:
  name: Get Exchange Rates
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseURL}}/rates"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
