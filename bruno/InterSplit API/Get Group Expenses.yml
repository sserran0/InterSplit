info:
  name: Get Group Expenses
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/groups/{{groupID}}/expenses"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
