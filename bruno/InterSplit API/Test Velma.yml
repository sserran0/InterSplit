info:
  name: Test Velma
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
