info:
  name: Test Velma
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "velma@test.com",
        "name": "Velma",
        "password": "test12356",
        "preferred_currency": "GBP"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
