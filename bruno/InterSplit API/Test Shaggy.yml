info:
  name: Test Shaggy
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "shaggy1@test.com",
        "name": "Shaggy",
        "password": "test12345",
        "preferred_currency": "MXN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
